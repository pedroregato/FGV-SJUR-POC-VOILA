services:
  sjur-app:
    build: .
    container_name: sjur-poc
    ports:
      - "8501:8501"
    volumes:
      - ./data:/app/data            # <-- banco de dados local
      - ./logs:/app/logs
    environment:
      - PYTHONUNBUFFERED=1
      - TZ=America/Sao_Paulo
    restart: unless-stopped
